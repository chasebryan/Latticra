name: macos-reset-uninstall-closeout-audit5-review-closeout-audit

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  closeout-audit5-review-closeout-audit-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Validate no-effect closeout audit contract
        run: |
          script='scripts/test-macos-reset-uninstall-live-runner-acceptance-denial'
          script="${script}-disposition-closeout-audit-review-disposition"
          script="${script}-closeout-audit-review-disposition"
          script="${script}-closeout-audit-review-disposition"
          script="${script}-closeout-audit-review-disposition"
          script="${script}-closeout-audit5-review-closeout-audit.sh"
          sh "$script"
