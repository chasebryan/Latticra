name: Fedora VM CLI Payload Repeatability Publication Receipt Acceptance Template

on:
  push:
    paths:
      - 'docs/FEDORA_VM_CLI_PAYLOAD_REPEATABILITY_PUBLICATION_RECEIPT_ACCEPTANCE_TEMPLATE.md'
      - 'docs/status/FEDORA_VM_CLI_PAYLOAD_REPEATABILITY_PUBLICATION_RECEIPT_ACCEPTANCE_TEMPLATE_STATUS.md'
      - 'scripts/fedora-vm-cli-payload-repeatability-publication-receipt-acceptance-template.sh'
      - 'scripts/test-fedora-vm-cli-payload-repeatability-publication-receipt-acceptance-template.sh'
      - '.github/workflows/fedora-vm-cli-payload-repeatability-publication-receipt-acceptance-template.yml'
  pull_request:

permissions:
  contents: read

jobs:
  fedora-vm-cli-payload-repeatability-publication-receipt-acceptance-template:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Run Fedora VM CLI payload repeatability publication receipt acceptance template guard
        run: sh scripts/test-fedora-vm-cli-payload-repeatability-publication-receipt-acceptance-template.sh
