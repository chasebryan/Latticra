name: Fedora Local Mock Build Gate Contract

on:
  pull_request:
    paths:
      - "scripts/test-fedora-local-mock-build-gate-contract.sh"
  push:
    branches:
      - main
    paths:
      - "scripts/test-fedora-local-mock-build-gate-contract.sh"

permissions:
  contents: read

jobs:
  fedora-local-mock-build-gate-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Run Fedora local mock build gate contract guard
        run: sh scripts/test-fedora-local-mock-build-gate-contract.sh
