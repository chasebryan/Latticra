name: Debian FreeBSD OpenBSD Package Payload Inspection Contract

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  debian-freebsd-openbsd-package-payload-inspection-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Run Debian FreeBSD OpenBSD package payload inspection contract guard
        run: sh scripts/test-debian-freebsd-openbsd-package-payload-inspection-contract.sh
