name: macos-reset-uninstall-closeout-audit5-review-disposition-closeout-audit

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  closeout-audit5-review-disposition-closeout-audit-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Validate no-effect closeout audit5 review disposition closeout audit
        run: |
          sh scripts/test-macos-reset-uninstall-live-runner-closeout-audit5-review-disposition-closeout-audit.sh
