name: Fedora Local RPM Spec Draft Plan

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  fedora-local-rpm-spec-draft-plan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - name: Run Fedora local RPM spec draft plan guard
        run: sh scripts/test-fedora-local-rpm-spec-draft-plan.sh
