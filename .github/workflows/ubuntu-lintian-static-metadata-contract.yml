name: Ubuntu Lintian Static Metadata Contract

on:
  pull_request:
    paths:
      - "scripts/test-ubuntu-lintian-static-metadata-contract.sh"
  push:
    branches:
      - main
    paths:
      - "scripts/test-ubuntu-lintian-static-metadata-contract.sh"

permissions:
  contents: read

jobs:
  ubuntu-lintian-static-metadata-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Run Ubuntu lintian static metadata contract guard
        run: sh scripts/test-ubuntu-lintian-static-metadata-contract.sh
