name: Nucleus Task Report-Only Execution Refinement

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  nucleus-task-report-only-execution-refinement:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Run Nucleus report-only execution refinement guard
        run: sh scripts/test-nucleus-task-report-only-execution-refinement.sh
      - name: Run Nucleus task execution tests
        run: sh scripts/test-nucleus-task-execution.sh
