name: Ubuntu Package Notice Promotion Gate Contract

on:
  pull_request:
    paths:
      - "scripts/test-ubuntu-package-notice-promotion-gate-contract.sh"
  push:
    branches:
      - main
    paths:
      - "scripts/test-ubuntu-package-notice-promotion-gate-contract.sh"

permissions:
  contents: read

jobs:
  ubuntu-package-notice-promotion-gate-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Run Ubuntu package notice promotion gate contract guard
        run: sh scripts/test-ubuntu-package-notice-promotion-gate-contract.sh
