name: Kernel runtime entry syscall return view

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  kernel-runtime-entry-syscall-return-view:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Run kernel runtime-entry syscall-return-view guard
        run: sh scripts/test-kernel-runtime-entry-syscall-return-view.sh

      - name: Run kernel runtime-entry syscall-return-view report runner
        run: sh scripts/test-kernel-runtime-entry-syscall-return-view-report-runner.sh
