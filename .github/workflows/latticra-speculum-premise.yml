name: Latticra Speculum Premise

on:
  pull_request:
    paths:
      - "scripts/test-latticra-speculum-premise.sh"
  push:
    branches:
      - main
    paths:
      - "scripts/test-latticra-speculum-premise.sh"

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Run Latticra Speculum premise guard
        run: sh scripts/test-latticra-speculum-premise.sh
