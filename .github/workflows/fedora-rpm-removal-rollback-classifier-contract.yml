name: Fedora RPM Removal Rollback Classifier Contract

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  fedora-rpm-removal-rollback-classifier-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - name: Run Fedora RPM removal rollback classifier contract guard
        run: sh scripts/test-fedora-rpm-removal-rollback-classifier-contract.sh
