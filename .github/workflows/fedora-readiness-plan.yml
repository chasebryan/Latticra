name: Fedora Readiness Plan

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  fedora-readiness-plan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run Fedora readiness plan guard
        run: sh scripts/test-fedora-readiness-plan.sh
