name: Kernel scheduler dispatch

on:
  pull_request:
    paths:
      - "scripts/test-kernel-scheduler-dispatch-report-runner.sh"
      - "scripts/test-kernel-scheduler-dispatch.sh"
  push:
    branches:
      - main
    paths:
      - "scripts/test-kernel-scheduler-dispatch-report-runner.sh"
      - "scripts/test-kernel-scheduler-dispatch.sh"

permissions:
  contents: read

jobs:
  kernel-scheduler-dispatch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Run kernel scheduler dispatch guard
        run: sh scripts/test-kernel-scheduler-dispatch.sh

      - name: Run kernel scheduler dispatch report runner
        run: sh scripts/test-kernel-scheduler-dispatch-report-runner.sh
