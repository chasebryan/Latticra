name: Fedora Developer Workflow

on:
  pull_request:
    paths:
      - "scripts/test-fedora-developer-workflow.sh"
  push:
    branches:
      - main
    paths:
      - "scripts/test-fedora-developer-workflow.sh"

permissions:
  contents: read

jobs:
  fedora-developer-workflow:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Run Fedora developer workflow guard
        run: sh scripts/test-fedora-developer-workflow.sh
