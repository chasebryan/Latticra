name: Fedora Manual Host RC Checklist Status Alignment

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  fedora-manual-host-rc-checklist-status-alignment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Fedora manual host RC checklist status alignment guard
        run: sh scripts/test-fedora-manual-host-rc-checklist-status-alignment.sh
