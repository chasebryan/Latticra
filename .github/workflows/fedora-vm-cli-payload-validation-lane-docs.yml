name: Fedora VM CLI Payload Validation Lane Docs

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  fedora-vm-cli-payload-validation-lane-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - name: Run Fedora VM CLI payload validation lane docs guard
        run: sh scripts/test-fedora-vm-cli-payload-validation-lane-docs.sh
