name: Ubuntu Package License Promotion Gate Contract

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  ubuntu-package-license-promotion-gate-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Run Ubuntu package license promotion gate contract guard
        run: sh scripts/test-ubuntu-package-license-promotion-gate-contract.sh
