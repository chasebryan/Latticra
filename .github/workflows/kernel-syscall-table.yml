name: Kernel Syscall Table

on:
  pull_request:
    paths:
      - "scripts/test-kernel-syscall-table-report-runner.sh"
      - "scripts/test-kernel-syscall-table.sh"
  push:
    branches:
      - main
    paths:
      - "scripts/test-kernel-syscall-table-report-runner.sh"
      - "scripts/test-kernel-syscall-table.sh"

permissions:
  contents: read

jobs:
  kernel-syscall-table:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Run kernel syscall table guard
        run: sh scripts/test-kernel-syscall-table.sh
      - name: Run kernel syscall table report runner guard
        run: sh scripts/test-kernel-syscall-table-report-runner.sh
