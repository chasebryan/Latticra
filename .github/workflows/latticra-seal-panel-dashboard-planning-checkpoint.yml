name: Latticra Seal Panel Dashboard Planning Checkpoint

on:
  pull_request:
    paths:
      - "scripts/test-latticra-seal-panel-dashboard-planning-checkpoint.sh"
  push:
    branches:
      - main
    paths:
      - "scripts/test-latticra-seal-panel-dashboard-planning-checkpoint.sh"

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - run: sh scripts/test-latticra-seal-panel-dashboard-planning-checkpoint.sh
