name: Lat-to-LIR Lowering

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  lat-to-lir-lowering:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run Lat-to-LIR lowering tests
        run: sh scripts/test-lat-to-lir-lowering.sh
