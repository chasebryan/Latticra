name: Lat Pipeline Diagnostic Integration

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sh scripts/test-lat-pipeline-diagnostic-integration-refinement.sh
      - run: sh scripts/test-lat-pipeline.sh
