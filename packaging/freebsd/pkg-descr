Latticra is an early-stage contract-first systems architecture and language
project.

This local-only FreeBSD port draft records package shape only and installs
the no-effect CLI status surface. It is not FreeBSD ports tree submission
evidence, committer review evidence, poudriere evidence, or package
readiness evidence.
